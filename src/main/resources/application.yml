server:
  port: 9090
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ZebraAssignment
    username: postgres
    password: asd123asd
  jpa:
    hibernate:
      ddl-auto: update
